# Secret holding credentials for the notification service.
# NOTE: values here are demo defaults (base64 of "securebank"/"guest"). In a real
# cluster these come from a sealed-secret / external secrets manager, NOT git.
apiVersion: v1
kind: Secret
metadata:
  name: notification-service-secret
  labels:
    app: notification-service
type: Opaque
data:
  # echo -n 'securebank' | base64
  SPRING_DATASOURCE_USERNAME: c2VjdXJlYmFuaw==
  SPRING_DATASOURCE_PASSWORD: c2VjdXJlYmFuaw==
  # echo -n 'guest' | base64
  SPRING_RABBITMQ_USERNAME: Z3Vlc3Q=
  SPRING_RABBITMQ_PASSWORD: Z3Vlc3Q=
